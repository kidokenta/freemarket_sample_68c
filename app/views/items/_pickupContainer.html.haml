-# 全ての商品一覧から最新を表示している
.pickupContainer
  %h2.productHead
    ピックアップカテゴリー
  %h2.productHead__new-item
    = link_to '新規投稿商品', '#', class: "new-item-link"
  .productLists
    - @new_items.each do |item|
      .productList
        =link_to item_path(item.id), class: "item-card-link" do
          .productList-img
            =image_tag "#{new_image(item).image}", class: "new-item"
          .productList-body
            %h2.item-title
              =item.name
            .productList-body__detail
              %ul
                %li.item
                  = number_to_currency(item.price)
                %li.item (税込)
              .productList-body__like
                %p 0
                %p.like-star ★

.pickupContainer
  %h2.productHead
    ピックアップブランド
  %h2.productHead__new-item
    = link_to 'アーカイバ', '#', class: "new-item-link"
  .productLists
    - @new_items.each do |item|
      .productList
        =link_to item_path(item.id), class: "item-card-link" do
          .productList-img
            =image_tag "#{new_image(item).image}", class: "new-item"
          .productList-body
            %h2.item-title
              =item.name
            .productList-body__detail
              %ul
                %li.item
                  = number_to_currency(item.price)
                %li.item (税込)
              .productList-body__like
                %p 0
                %p.like-star ★